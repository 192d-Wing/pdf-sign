{
  "manifest_version": 3,
  "name": "pdf-sign smart card bridge",
  "description": "Relays PDF signing digests between the pdf-sign web app and the local smart card (Windows CNG) via native messaging.",
  "version": "0.1.0",
  "permissions": ["nativeMessaging"],
  "background": {
    "service_worker": "background.js"
  },
  "content_scripts": [
    {
      "matches": [
        "http://127.0.0.1:8080/*",
        "http://localhost:8080/*"
      ],
      "js": ["content.js"],
      "run_at": "document_start"
    }
  ]
}
