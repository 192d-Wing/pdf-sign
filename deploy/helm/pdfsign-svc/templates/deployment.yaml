apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "pdfsign-svc.fullname" . }}
  labels:
    {{- include "pdfsign-svc.labels" . | nindent 4 }}
  # checkov:skip=CKV_K8S_43:Pin by digest via values.image.digest at deploy time.
  # checkov:skip=CKV_K8S_21:Helm installs into the release namespace (helm -n); templates intentionally omit it.
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "pdfsign-svc.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "pdfsign-svc.selectorLabels" . | nindent 8 }}
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: pdfsign-svc
          {{- if .Values.image.digest }}
          image: "{{ .Values.image.repository }}@{{ .Values.image.digest }}"
          {{- else }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          {{- end }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - -addr
            - 0.0.0.0:{{ .Values.listenPort }}
            - -max-sessions
            - {{ .Values.maxSessions | quote }}
            {{- if .Values.tsaURL }}
            - -tsa
            - {{ .Values.tsaURL }}
            {{- end }}
            {{- if .Values.dev.enabled }}
            - -dev
            {{- else }}
            - -sign-ca
            - /etc/pdfsign/cas/sign-ca.pem
            - -client-ca
            - /etc/pdfsign/cas/client-ca.pem
            - -tls-cert
            - /etc/pdfsign/tls/tls.crt
            - -tls-key
            - /etc/pdfsign/tls/tls.key
            {{- end }}
          {{- if and .Values.dev.enabled .Values.dev.tokenSecretName }}
          env:
            - name: PDFSIGN_DEV_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.dev.tokenSecretName }}
                  key: token
          {{- end }}
          ports:
            - name: https
              containerPort: {{ .Values.listenPort }}
          # TCP probes: an HTTPS probe cannot pass the handshake because the
          # server requires client certificates.
          readinessProbe:
            tcpSocket:
              port: https
            initialDelaySeconds: 2
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: https
            initialDelaySeconds: 5
            periodSeconds: 20
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- if not .Values.dev.enabled }}
          volumeMounts:
            - name: tls
              mountPath: /etc/pdfsign/tls
              readOnly: true
            - name: cas
              mountPath: /etc/pdfsign/cas
              readOnly: true
          {{- end }}
      {{- if not .Values.dev.enabled }}
      volumes:
        - name: tls
          secret:
            secretName: {{ .Values.tls.secretName }}
        - name: cas
          secret:
            secretName: {{ .Values.cas.secretName }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
